import React from "react";
import { Campaign } from "../types";

const AdminPanel: React.FC<any> = ({
  appState,
  setAppState,
  adminTab,
  setAdminTab
}) => {
  return (
    <div className="pb-40">
      {/* ADMIN JSX — handleFile, handleCreateCampaign, dashboard etc */}
    </div>
  );
};

export default AdminPanel;
